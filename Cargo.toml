[workspace]
members = ["daemon", "applet"]
resolver = "2"

[workspace.package]
edition = "2024"
license = "GPL-3.0-only"
version = "2.0.1"

[profile.release]
opt-level = 3
lto = "thin"
panic = "abort"
